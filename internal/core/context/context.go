package context

// context constants
// ctx.Locals() 메서드에서 주로 사용됨
const (
	Container    = "container"
	App          = "app"
	DB           = "db"
	Config       = "config"
	Logger       = "logger"
	AuthUser     = "authUser"
	JwtGenerator = "jwtGenerator"
	Permissions  = "permissions"
)
